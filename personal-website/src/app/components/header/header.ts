import { Component, signal } from '@angular/core';
import { Nav } from '../nav/nav';


@Component({
  imports: [Nav],
  selector: 'app-header',
  styleUrl: './header.css',
  templateUrl: './header.html',
})
export class Header {
  
}
